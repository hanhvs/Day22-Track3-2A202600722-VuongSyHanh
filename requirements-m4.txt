# Apple Silicon (M1/M2/M3/M4) tier — MPS / Metal backend.
# NO unsloth, NO bitsandbytes: neither has an MPS backend. We run plain
# transformers + peft + trl in fp32 over torch's `mps` device.
# See HARDWARE-GUIDE.md §8 and colab/Lab22_DPO_M4.ipynb.

# ── Core training stack (MPS-compatible) ──────────────────────
torch>=2.4                              # MPS backend; 2.8+ recommended
transformers>=4.46,<5.0
trl>=0.12,<0.20                         # DPOTrainer + DPOConfig
peft>=0.13,<1.0                         # plain LoraConfig (no Unsloth)
accelerate>=1.1,<2.0
datasets>=3.1,<4.0

# ── Plotting + reporting ──────────────────────────────────────
matplotlib>=3.9,<4.0
pandas>=2.2,<3.0
pyarrow>=17,<22

# ── GGUF export (notebook NB5) ────────────────────────────────
# llama-cpp-python compiles llama.cpp with Metal on macOS by default.
# convert_hf_to_gguf.py (from the cloned llama.cpp repo) needs gguf + sentencepiece.
llama-cpp-python>=0.3,<1.0
gguf>=0.10
sentencepiece>=0.2
protobuf>=4

# ── Optional API judges (notebook NB4) ────────────────────────
openai>=1.55,<2.0
anthropic>=0.40,<1.0

# ── Notebook + test ───────────────────────────────────────────
jupyterlab>=4.3,<5.0
jupytext>=1.16,<2.0
pytest>=8.3,<9.0
